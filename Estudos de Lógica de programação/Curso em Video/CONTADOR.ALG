Algoritmo "Contador"

Var
  Contador, N: inteiro
Inicio
      Escreval("Quantos numeros deseja contar?")
      Leia(N)
      Contador <- 0
      Enquanto (Contador <= N) faça
      Escreval(Contador, ";")
      Contador <- Contador + 1
      FimEnquanto

Fimalgoritmo